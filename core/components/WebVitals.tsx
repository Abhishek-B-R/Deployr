"use client"

// This component is intentionally a no-op to avoid build-time deps.
// Web Vitals are loaded via next/script in app/layout.tsx using the web-vitals IIFE from a CDN.
export default function WebVitals() {
  return null
}
